package inheritance;

public class PolyTest {

	public static void main(String[] args) {
		 Nissan n=new Nissan();
		 n.turn(10);//turn method call
		 // more than 1 turn method
		 // method call conflict is resolved
		 // before running(compile) the program
	}
	// write code
	// compile code = english code converted to binary code
	// run code = launching program 

}
